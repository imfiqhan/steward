# binaries
/steward
/example/example
/cmd/steward/steward
/tools/assets/assets

# local data
*.db
*.sqlite
uploads/

# tooling
.DS_Store
coverage.out
frontend/.bin/
frontend/node_modules/
node_modules/
package-lock.json
.claude/

# note: assets/dist/ stays committed — go:embed ships it in the binary

# Scratch browser probes: written per investigation, never part of the repo.
scripts/tmp*.mjs
